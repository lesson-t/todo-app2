= link_to task_path(id: task.id) do
  .card
    .card_content
      .card_title
        = task.title
      .card_detail
        = image_tag board.user.avatar_image
        %div
          %p= board.author_name